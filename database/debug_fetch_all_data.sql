-- Fetch all data from the `profiles` table
SELECT * FROM profiles;

-- Fetch all data from the `regions` table
SELECT * FROM regions;

-- Fetch all data from the `provinces` table
SELECT * FROM provinces;

-- Fetch all data from the `chatrooms` table
SELECT * FROM chatrooms;

-- Fetch all data from the `messages` table
SELECT * FROM messages;

-- Add more queries for other tables as needed
-- Example:
-- SELECT * FROM your_table_name;
